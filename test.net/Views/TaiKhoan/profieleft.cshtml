@using CuaHangDAL;

<div class="profile_details">
    <ul>
        <li class="dropdown profile_details_drop">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                <div class="profile_img">
                    <span class="prfil-img"><i class="fa fa-user" aria-hidden="true"></i></span>
                    <div class="clearfix"></div>
                </div>
            </a>

            <ul class="dropdown-menu drp-mnu">
                @if (Session["USER_SESSION"] != null)
                {
                    TaiKhoan tk = (TaiKhoan)Session["USER_SESSION"];

                    <li> <a href="#"><i class="fa fa-cog"></i> @tk.TenTK</a> </li>
                    <li> <a href="#"><i class="fa fa-user"></i> @tk.Loaitk</a> </li>
                    <li> <a href="/TaiKhoan/Logout"><i class="fa fa-sign-out"></i> Logout</a> </li>
                   @*<li> @Html.ActionLink("Logout", "Logout", "TaiKhoan",new { @class= "fa fa-sign-out"})</li>*@
                   }

            </ul>
        </li>
    </ul>
</div>
